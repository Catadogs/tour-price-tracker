---
phase: 01-sqlite-state-and-single-container-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - price_monitor/storage.py
  - tests/test_storage.py
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - OPS-04
  - QUAL-02
must_haves:
  truths:
    - "A fresh SQLite database file initializes without manual SQL."
    - "Legacy JSON settings, latest snapshots, and price history import once when present."
    - "Runtime settings, latest snapshots, and price history round-trip through SQLite tables."
    - "A corrupt SQLite database is renamed and replaced without deleting the bad file."
  artifacts:
    - path: "price_monitor/storage.py"
      provides: "SQLite schema, connection handling, JSON import, and storage facade"
    - path: "tests/test_storage.py"
      provides: "Focused SQLite initialization, migration, persistence, and corruption tests"
  key_links:
    - from: "tests/test_storage.py"
      to: "price_monitor/storage.py"
      via: "direct imports of initialize_storage and persistence helpers"
      pattern: "from price_monitor import storage"
    - from: "price_monitor/storage.py"
      to: "metadata.json_import_completed"
      via: "one-time migration guard"
      pattern: "json_import_completed"
---

<objective>
Create the SQLite storage foundation for Phase 1.

Purpose: Move the durable storage substrate to stdlib SQLite while preserving one-process operation and creating a small API that `monitor.py` can use in the next plan.
Output: `price_monitor/storage.py` plus focused tests in `tests/test_storage.py`.
</objective>

<execution_context>
@C:/Users/Reshape Team/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Reshape Team/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sqlite-state-and-single-container-foundation/01-CONTEXT.md
@.planning/phases/01-sqlite-state-and-single-container-foundation/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@price_monitor/monitor.py

<interfaces>
Create `price_monitor/storage.py` with this small path-based API. Do not import `MonitorConfig` into `storage.py`.

```python
def initialize_storage(
    db_path: Path,
    *,
    settings_path: Path | None = None,
    state_path: Path | None = None,
    history_path: Path | None = None,
) -> None: ...
def load_runtime_settings(db_path: Path) -> dict[str, object]: ...
def save_runtime_settings(db_path: Path, settings: dict[str, object]) -> None: ...
def load_snapshot(db_path: Path) -> dict[str, dict[str, object]]: ...
def save_snapshot(db_path: Path, data: dict[str, dict[str, object]]) -> None: ...
def load_price_history(db_path: Path) -> dict[str, list[list]]: ...
def append_price_history(
    db_path: Path,
    current_snapshot: dict[str, dict[str, object]],
    observed_at: str,
) -> None: ...
```

Required tables: `metadata`, `runtime_settings`, `latest_snapshots`, `price_history`.
Required metadata keys: `schema_version`, `json_import_completed`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build SQLite schema and initialization path</name>
  <files>price_monitor/storage.py, tests/test_storage.py</files>
  <read_first>
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-CONTEXT.md
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-RESEARCH.md
    .planning/codebase/CONVENTIONS.md
    price_monitor/monitor.py
  </read_first>
  <behavior>
    - `initialize_storage(tmp_path / "price_monitor.sqlite3")` creates parent directories, the SQLite file, and the four required tables.
    - `metadata` contains `schema_version = 1` after initialization.
    - Missing legacy JSON paths are accepted and still set `json_import_completed = 1`.
    - The implementation uses stdlib `sqlite3` only; do not add SQLAlchemy, Alembic, brokers, workers, or database services.
  </behavior>
  <action>
    Create `price_monitor/storage.py` per D-09, D-13, and D-14. Use `sqlite3.connect(db_path, timeout=10)`, `sqlite3.Row`, `PRAGMA foreign_keys = ON`, `PRAGMA busy_timeout = 10000`, and either WAL mode with sidecar handling or default journal mode with the same timeout. Create tables:
    `metadata(key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at TEXT NOT NULL)`;
    `runtime_settings(key TEXT PRIMARY KEY, value_type TEXT NOT NULL, value_json TEXT NOT NULL, updated_at TEXT NOT NULL)`;
    `latest_snapshots(snapshot_key TEXT PRIMARY KEY, target_name TEXT NOT NULL, provider TEXT NOT NULL, departure_date TEXT NOT NULL, nights INTEGER NOT NULL, offer_identity TEXT NOT NULL, price_rub INTEGER NOT NULL, observed_at TEXT NOT NULL, payload_json TEXT NOT NULL)`;
    `price_history(id INTEGER PRIMARY KEY AUTOINCREMENT, snapshot_key TEXT NOT NULL, target_name TEXT NOT NULL, provider TEXT NOT NULL, departure_date TEXT NOT NULL, nights INTEGER NOT NULL, price_rub INTEGER NOT NULL, observed_at TEXT NOT NULL, payload_json TEXT NOT NULL)`.
    Add indexes `idx_latest_snapshots_lookup` and `idx_price_history_lookup` on `(target_name, provider, departure_date, nights, observed_at)`. Add a module-level `threading.RLock` around writes to preserve one-process threaded safety.
  </action>
  <verify>
    <automated>python -m pytest tests/test_storage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_storage.py -q` passes.
    - `Select-String -Path price_monitor/storage.py -Pattern "CREATE TABLE IF NOT EXISTS metadata","CREATE TABLE IF NOT EXISTS runtime_settings","CREATE TABLE IF NOT EXISTS latest_snapshots","CREATE TABLE IF NOT EXISTS price_history","sqlite3.connect","threading.RLock"` returns matches.
    - `Select-String -Path price_monitor/storage.py -Pattern "sqlalchemy|alembic|redis|celery|psycopg|mysql" -CaseSensitive` returns no matches.
  </acceptance_criteria>
  <done>Fresh SQLite initialization is idempotent, creates the required schema, and has focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement legacy JSON import and persistence operations</name>
  <files>price_monitor/storage.py, tests/test_storage.py</files>
  <read_first>
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-CONTEXT.md
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-RESEARCH.md
    .planning/codebase/ARCHITECTURE.md
    price_monitor/monitor.py
    tests/test_price_monitor.py
  </read_first>
  <behavior>
    - Legacy settings JSON imports from `settings_path` into `runtime_settings`.
    - Legacy latest snapshot JSON imports from `state_path` into `latest_snapshots`.
    - Legacy price history JSON imports from `history_path` into append-only `price_history` rows.
    - Re-running initialization after SQLite writes does not re-import or overwrite newer SQLite state because `json_import_completed = 1`.
    - Malformed legacy JSON logs an actionable warning, skips that input, and still completes initialization.
  </behavior>
  <action>
    Implement D-05, D-06, D-07, D-08, D-10, D-11, and D-12. In `initialize_storage`, import JSON only when `metadata` has no `json_import_completed` row. Store setting values as JSON per key with `value_type` set to the Python type name. Store complete snapshot and history item dictionaries in `payload_json` using `json.dumps(..., ensure_ascii=False, sort_keys=True)` while also extracting typed columns. Derive `target_name` from the snapshot key before the first `|`; derive `offer_identity` from the remaining key text; use `provider` from payload when present, otherwise `Biblio-Globus` for normal offers and `external` for keys ending in `|external`. `load_price_history` must return the current compatibility shape `dict[str, list[list]]` sorted by insertion order, with each list entry `[observed_at, price_rub]`. `append_price_history` must insert rows and must not rewrite the full history table.
  </action>
  <verify>
    <automated>python -m pytest tests/test_storage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_storage.py -q` passes with tests named for one-time import, settings round trip, snapshot round trip, history append, and malformed legacy JSON.
    - `Select-String -Path tests/test_storage.py -Pattern "json_import_completed","load_runtime_settings","save_runtime_settings","load_snapshot","save_snapshot","load_price_history","append_price_history"` returns matches.
    - `Select-String -Path price_monitor/storage.py -Pattern "json.loads","logging.warning","INSERT INTO price_history","ON CONFLICT"` returns matches.
  </acceptance_criteria>
  <done>Legacy JSON is a one-time migration input only, and SQLite persistence operations round-trip the existing data shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add corrupt database quarantine fallback</name>
  <files>price_monitor/storage.py, tests/test_storage.py</files>
  <read_first>
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-CONTEXT.md
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-RESEARCH.md
    .planning/codebase/CONCERNS.md
    price_monitor/monitor.py
  </read_first>
  <behavior>
    - If the SQLite file cannot be opened or fails `PRAGMA quick_check`, it is renamed to `price_monitor.sqlite3.corrupt-YYYYMMDDHHMMSS`.
    - If `price_monitor.sqlite3-wal` or `price_monitor.sqlite3-shm` exists, sidecars are renamed with the same corrupt suffix.
    - After quarantine, a new empty database is initialized and the bad database file is not deleted.
    - An actionable warning is logged with the corrupt file path and replacement database path.
  </behavior>
  <action>
    Implement D-18, D-19, and D-20 in `initialize_storage`. Catch `sqlite3.DatabaseError` and `sqlite3.OperationalError` around open/schema/quick_check. Quarantine the main file and WAL sidecars by renaming them in the same directory with `.corrupt-<timestamp>` appended to each original filename. Retry initialization once after quarantine; if the retry fails, let the exception propagate. Do not delete the corrupt database or legacy JSON inputs.
  </action>
  <verify>
    <automated>python -m pytest tests/test_storage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_storage.py -q` passes with a test named `test_corrupt_db_is_renamed_and_reinitialized`.
    - `Select-String -Path price_monitor/storage.py -Pattern "quick_check","corrupt-","Path(f\"{db_path}-wal\")","Path(f\"{db_path}-shm\")"` returns matches or equivalent sidecar handling code.
    - A test asserts the corrupt source file path no longer exists and at least one `*.corrupt-*` file exists.
  </acceptance_criteria>
  <done>Corrupt SQLite files are preserved for recovery and the service can continue with a new initialized database.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_storage.py -q`.
</verification>

<success_criteria>
`price_monitor/storage.py` exists, uses stdlib SQLite, initializes schema idempotently, imports legacy JSON once, supports settings/snapshot/history persistence, and quarantines corrupt databases with passing focused tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sqlite-state-and-single-container-foundation/01-01-SUMMARY.md`.
</output>
