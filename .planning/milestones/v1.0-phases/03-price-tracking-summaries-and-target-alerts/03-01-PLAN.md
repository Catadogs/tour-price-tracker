---
phase: 03-price-tracking-summaries-and-target-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_price_monitor.py
autonomous: true
requirements:
  - TG-08
  - TRK-01
  - TRK-02
  - TRK-03
  - TRK-04
  - TRK-05
  - TRK-06
must_haves:
  truths:
    - "format_report output includes target name, departure range, nights, filters, best price, and per-date breakdown."
    - "find_overall_best selects the cheapest offer across all dates and nights."
    - "snapshot produces identity-keyed dict from best offers."
    - "format_changes detects new offers, price increases, and price decreases."
    - "format_new_minimums detects when current price is below historical minimum."
    - "format_target_alerts detects when any offer meets or beats target price."
    - "adaptive_interval reduces interval when departure is close."
    - "All existing 32 tests continue to pass."
  artifacts:
    - path: "tests/test_price_monitor.py"
      provides: "Tests for report formatting, change detection, alerts, and scheduling"
  key_links:
    - from: "tests/test_price_monitor.py"
      to: "price_monitor/monitor.py"
      via: "imports of format_report, format_changes, format_new_minimums, format_target_alerts, find_overall_best, snapshot, adaptive_interval"
      pattern: "from price_monitor.monitor import ..."
---

<objective>
Add test coverage for the price tracking, reporting, and alerting functions that already exist in `price_monitor/monitor.py`.

Phase 3 functionality is fully implemented: periodic checks, manual checks, filtering, best-price selection, target alerts, and historical minimums. This plan adds focused tests to verify correct behavior.
</objective>

<execution_context>
### Task 1: Report formatting tests (TG-08, TRK-04)
- `test_format_report_includes_all_sections` — verify output has target name, hotel, departure, nights, best price, per-date breakdown
- `test_format_report_handles_empty_best` — no matching offers shows warning
- `test_find_overall_best_selects_cheapest` — across dates and nights
- `test_snapshot_creates_identity_keys` — keys match "{target}|{departure}|{nights}|{room}"

### Task 2: Alert and change tests (TRK-05, TRK-06)
- `test_format_changes_detects_new_offers` — 🆕 for new identity
- `test_format_changes_detects_price_increase` — 📈 with +diff
- `test_format_changes_detects_price_decrease` — 📉 with -diff  
- `test_format_changes_returns_none_for_empty_previous` — first run suppression
- `test_format_new_minimums_detects_historical_low` — price below history min
- `test_format_new_minimums_skips_when_not_minimum` — price above/equal to history min
- `test_format_target_alerts_detects_target_met` — price ≤ target
- `test_format_target_alerts_skips_above_target` — price > target

### Task 3: Scheduling test (TRK-01)
- `test_adaptive_interval_reduces_when_close` — <14 days → 900s max, <30 days → 3600s max

### Verification
- Run full test suite: `python -m pytest tests/ -v`
- All 32 existing + ~11 new tests must pass
</execution_context>

<complexity_track>
Why not simpler: Tests are straightforward unit tests for pure functions.
Why not more complex: No code changes needed — all functionality already implemented.
</complexity_track>
