---
phase: 01-sqlite-state-and-single-container-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - README.md
autonomous: true
requirements:
  - STOR-01
  - STOR-05
  - OPS-01
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "Docker Compose exposes BG_DB_PATH=/data/price_monitor.sqlite3 to the single service container."
    - "The existing Docker volume persists the SQLite database under /data."
    - "Legacy JSON paths remain configured only as migration inputs."
    - "The Compose file still defines one application service and no broker, worker, or heavy database service."
  artifacts:
    - path: "docker-compose.yml"
      provides: "One-container runtime environment and /data volume wiring"
    - path: ".env.example"
      provides: "Example BG_DB_PATH and legacy migration input paths"
    - path: "README.md"
      provides: "User-facing SQLite runtime path documentation"
  key_links:
    - from: "docker-compose.yml"
      to: "/data/price_monitor.sqlite3"
      via: "BG_DB_PATH environment variable"
      pattern: "BG_DB_PATH.*price_monitor\\.sqlite3"
    - from: "docker-compose.yml"
      to: "bg-price-monitor-data"
      via: "volume mounted at /data"
      pattern: "bg-price-monitor-data:/data"
---

<objective>
Wire the one-container Docker runtime for SQLite persistence.

Purpose: Make the default container runtime use `/data/price_monitor.sqlite3` while preserving old JSON path variables strictly as migration inputs.
Output: Updated Compose, example env, and README runtime notes.
</objective>

<execution_context>
@C:/Users/Reshape Team/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Reshape Team/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sqlite-state-and-single-container-foundation/01-CONTEXT.md
@.planning/phases/01-sqlite-state-and-single-container-foundation/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@docker-compose.yml
@.env.example
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLite database path to Compose and env example</name>
  <files>docker-compose.yml, .env.example</files>
  <read_first>
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-CONTEXT.md
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-RESEARCH.md
    docker-compose.yml
    .env.example
  </read_first>
  <action>
    Implement D-01, D-02, D-03, and D-04 in runtime configuration. In `docker-compose.yml`, add `BG_DB_PATH: ${BG_DB_PATH:-/data/price_monitor.sqlite3}` under the existing `bg-price-monitor.environment` map. Also add `BG_SETTINGS_PATH: /data/settings.json` so settings JSON remains an explicit migration input alongside existing `BG_STATE_PATH: /data/last_snapshot.json` and `BG_HISTORY_PATH: /data/price_history.json`. Keep the existing single service `bg-price-monitor`, existing `bg-price-monitor-data:/data` mount, existing Dockerfile, and existing command. In `.env.example`, add `BG_DB_PATH=/data/price_monitor.sqlite3`, `BG_STATE_PATH=/data/last_snapshot.json`, and `BG_HISTORY_PATH=/data/price_history.json` near `BG_SETTINGS_PATH`, with no secret values.
  </action>
  <verify>
    <automated>docker compose config --services</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --services` prints only `bg-price-monitor`.
    - `Select-String -Path docker-compose.yml -Pattern "BG_DB_PATH","/data/price_monitor.sqlite3","BG_SETTINGS_PATH","BG_STATE_PATH","BG_HISTORY_PATH","bg-price-monitor-data:/data"` returns matches.
    - `Select-String -Path .env.example -Pattern "BG_DB_PATH=/data/price_monitor.sqlite3","BG_SETTINGS_PATH=/data/settings.json","BG_STATE_PATH=/data/last_snapshot.json","BG_HISTORY_PATH=/data/price_history.json"` returns matches.
    - `Select-String -Path docker-compose.yml -Pattern "redis|rabbit|kafka|celery|postgres|mysql|worker" -CaseSensitive` returns no matches.
  </acceptance_criteria>
  <done>Compose and `.env.example` expose the SQLite DB path while retaining legacy JSON inputs only for migration.</done>
</task>

<task type="auto">
  <name>Task 2: Document the SQLite runtime path and simple deployment model</name>
  <files>README.md</files>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-sqlite-state-and-single-container-foundation/01-CONTEXT.md
    README.md
  </read_first>
  <action>
    Update README runtime wording so it says the service stores settings, latest snapshots, and price history in SQLite at `BG_DB_PATH`, defaulting to `/data/price_monitor.sqlite3` in the mounted Docker volume. Replace the current statement that Telegram settings are stored at `/data/settings.json` with a statement that old JSON files at `BG_SETTINGS_PATH`, `BG_STATE_PATH`, and `BG_HISTORY_PATH` are migration inputs on first SQLite initialization. Keep the quick-start commands as `docker compose run --rm -e BG_RUN_ONCE=1 bg-price-monitor`, `docker compose up -d --build`, `docker logs -f bg-price-monitor`, and `docker compose down`. Do not add Kubernetes, CI, broker, worker, PostgreSQL, MySQL, Redis, or web dashboard instructions.
  </action>
  <verify>
    <automated>docker compose config --services</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --services` prints only `bg-price-monitor`.
    - `Select-String -Path README.md -Pattern "BG_DB_PATH","/data/price_monitor.sqlite3","SQLite","BG_SETTINGS_PATH","BG_STATE_PATH","BG_HISTORY_PATH"` returns matches.
    - `Select-String -Path README.md -Pattern "Kubernetes|Celery|Redis|RabbitMQ|PostgreSQL|MySQL|web dashboard|worker container" -CaseSensitive` returns no matches.
  </acceptance_criteria>
  <done>README documents the SQLite volume path and does not introduce any out-of-scope deployment architecture.</done>
</task>

</tasks>

<verification>
Run `docker compose config --services` and inspect that the only service is `bg-price-monitor`.
</verification>

<success_criteria>
Docker runtime remains one service/container, the mounted `/data` volume contains `/data/price_monitor.sqlite3`, old JSON path variables are retained only for import, and no broker/heavy database service is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sqlite-state-and-single-container-foundation/01-03-SUMMARY.md`.
</output>
