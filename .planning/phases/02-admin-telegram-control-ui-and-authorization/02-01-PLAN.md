---
phase: 02-admin-telegram-control-ui-and-authorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - price_monitor/monitor.py
  - tests/test_price_monitor.py
autonomous: true
requirements:
  - QUAL-05
  - QUAL-06
must_haves:
  truths:
    - "normalize_search_url rejects lookalike domains like bgoperator.ru.evil.com and evil-bgoperator.ru."
    - "normalize_search_url continues to accept valid bgoperator.ru, level.travel, and travelata.ru URLs."
    - "is_authorized rejects non-admin chat ids when TELEGRAM_CHAT_ID is configured."
    - "is_authorized allows all chats when TELEGRAM_CHAT_ID is unset."
    - "main_keyboard and settings_keyboard return valid Telegram inline keyboard structures."
    - "format_settings output includes departure, nights, filters, diff threshold, interval, target price, and searches."
    - "All existing 25 tests continue to pass after URL validation changes."
  artifacts:
    - path: "price_monitor/monitor.py"
      provides: "Hardened normalize_search_url with exact host matching"
    - path: "tests/test_price_monitor.py"
      provides: "Tests for URL validation, authorization, keyboards, and format_settings"
  key_links:
    - from: "tests/test_price_monitor.py"
      to: "price_monitor/monitor.py"
      via: "direct imports of normalize_search_url, is_authorized, main_keyboard, settings_keyboard, format_settings"
      pattern: "from price_monitor.monitor import ..."
---

<objective>
Harden URL validation against lookalike domains and add Telegram-focused tests.

Phase 2 features already exist in code (keyboards, settings editing, authorization). This plan adds security hardening for URL validation and test coverage for the Telegram surface.
</objective>

<execution_context>
Two focused tasks:

### Task 1: Harden `normalize_search_url` (QUAL-06)
Replace substring matching `any(allowed in host for allowed in allowed_hosts)` with suffix/exact matching:
```python
def _is_allowed_host(host: str) -> bool:
    host = host.lower().removeprefix("www.")
    return host in {"bgoperator.ru", "level.travel", "travelata.ru"}
```
This rejects `bgoperator.ru.evil.com`, `evil-bgoperator.ru`, `not-bgoperator.ru`, etc.

### Task 2: Add Telegram tests (QUAL-05)
Add to `tests/test_price_monitor.py`:
- `test_normalize_search_url_rejects_lookalike_domains` — test bgoperator.ru.evil.com, evil-bgoperator.ru, fake-level.travel.evil.com
- `test_is_authorized_blocks_unrecognized_chat` — with TELEGRAM_CHAT_ID=123, chat 456 rejected
- `test_is_authorized_allows_all_when_chat_id_unset` — with TELEGRAM_CHAT_ID=None, any chat allowed
- `test_main_keyboard_structure` — verify inline_keyboard list with expected callback_data values
- `test_settings_keyboard_structure` — verify inline_keyboard list with expected callback_data values
- `test_format_settings_contains_key_fields` — verify output includes key setting names

### Verification
- Run full test suite: `python -m pytest tests/ -v`
- All 25 existing + ~6 new tests must pass
</execution_context>

<complexity_track>
Why not simpler: URL validation is a one-function fix; tests are straightforward unit tests.
Why not more complex: No new Telegram API interactions, no Docker changes, no new modules.
</complexity_track>
